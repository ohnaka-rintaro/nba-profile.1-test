<h2>NBA選手: <%= @profile.name %>のプロフィールを編集する</h2>

<%= form_for(@profile) do |f| %>
  <%= f.label :name, '名前' %>
  <%= f.text_field :name %>
  
  <%= f.label :content, '説明' %>
  <%= f.text_field :content %>
  
  <%= f.submit '投稿' %>
<% end %>
  
  <%= link_to 'NBA Profile 一覧に戻る', profiles_path %>